Move codegen out of AST
Move allocs to function head (get them out of loops, etc)
%/%%
++ --
Assign-arith casts:
  let x u8: 3;
  x *= 4;    // Error: x *= 4 equiv. x = x * 4; x * 4 is int, cannot assign
             // int to u8.
Volatile
